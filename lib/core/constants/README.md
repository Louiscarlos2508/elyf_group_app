# Core › Constants

- `firestore_paths.dart` – builders pour les collections multi-entreprises.
- `feature_flags.dart` – bascules distantes (Remote Config).
- `roles.dart` – rôles et permissions par module.
- `sync_keys.dart` – clés utilisées par Drift/Firestore pour la résolution.

