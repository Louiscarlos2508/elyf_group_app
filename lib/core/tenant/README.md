# Core › Tenant

- `tenant_repository.dart` – liste des entreprises disponibles.
- `tenant_provider.dart` – Riverpod `StateNotifier` pour l’entreprise active.
- `tenant_selector.dart` – widget UI + recherche, tri, favoris.
- `tenant_middlewares.dart` – garantit que les requêtes Firestore sont scoped.

