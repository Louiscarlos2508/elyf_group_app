# Core › Offline

- `isar_service.dart` – initialisation, migrations, instances.
- `sync_manager.dart` – stratégie offline-first + résolution `updated_at`.
- `models/isar/` – entités partagées (orders, stocks, tenants...).
- `jobs/` – planification des synchronisations et retries.

