# Core › Logging

- `logger.dart` – wrapper `dart:developer` + niveaux.
- `audit_channel.dart` – envoi Firestore + stockage local.
- `observers/` – observateurs Riverpod/Router pour suivre les actions.

