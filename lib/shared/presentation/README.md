# Shared › Presentation

- `screens/` – UI transverses (splash, erreurs, onboarding).
- `widgets/` – composants (badges, charts, search bar, sync badge).
- `layouts/` – shells adaptatifs multi-colonnes.

