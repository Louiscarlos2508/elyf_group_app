# Services Layer

Services applicatifs indépendants des features :

- `connectivity_service.dart` – surveille réseau & offline hints.
- `permission_service.dart` – demandes runtime (caméra, emplacement).
- `app_update_service.dart` – Remote Config + prompts de mise à jour.
- `notification_service.dart` – orchestration locale + FCM.

