/// Enum used for bottom navigation in the module shell.
enum EauMineraleSection {
  activity,
  production,
  sales,
  stock,
  clients,
  suppliers,
  purchases,
  finances,
  salaries,
  reports,
  profile,
  treasury,
  settings,
  catalog,
}
