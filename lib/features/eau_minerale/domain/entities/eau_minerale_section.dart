/// Enum used for bottom navigation in the module shell.
enum EauMineraleSection {
  activity,
  production,
  sales,
  stock,
  clients,
  finances,
  salaries,
  reports,
  treasury,
  profile,
  settings,
}

