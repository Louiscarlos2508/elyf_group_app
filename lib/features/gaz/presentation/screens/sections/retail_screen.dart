import 'package:flutter/material.dart';

class RetailScreen extends StatelessWidget {
  const RetailScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Retail Screen - À implémenter'),
    );
  }
}

