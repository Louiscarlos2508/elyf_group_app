import 'package:flutter/material.dart';

class WholesaleScreen extends StatelessWidget {
  const WholesaleScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Wholesale Screen - À implémenter'),
    );
  }
}

