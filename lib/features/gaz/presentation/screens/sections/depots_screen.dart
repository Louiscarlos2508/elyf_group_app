import 'package:flutter/material.dart';

class DepotsScreen extends StatelessWidget {
  const DepotsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Depots Screen - À implémenter'),
    );
  }
}

