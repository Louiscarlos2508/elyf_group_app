# Story 11.1: Database Schema & Entity Alignment

## Context
Defining the Drift tables and ensuring entities match the standard project patterns.

## Requirements
- Define Drift tables for `Property`, `Tenant`, `Contract`, `Payment`, and `PropertyExpense`.
- Ensure all entities include `enterpriseId`.
- Update converters for seamless Drift <-> Domain mapping.

## Implementation Details
Modify `lib/core/offline/drift_service.dart` and `sync_paths.dart`.
