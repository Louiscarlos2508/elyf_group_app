# Story 8.1: Interface Unification

## Context
Merging the capabilities of various permission services into a single unified interface.

## Requirements
- Support for `enterpriseId` in `PermissionService`.
- Unified method signatures for all permission checks.
- Deprecation planning for legacy interfaces.

## Implementation Details
Updated `PermissionService` in `lib/core/permissions/services/permission_service.dart`.
