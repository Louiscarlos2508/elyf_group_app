# Story 8.3: Module Provider Updates

## Context
Migrating all feature modules to the unified permission provider.

## Requirements
- Update `Administration`, `Gaz`, `Boutique`, `Orange Money`, and `Immobilier` modules.
- Ensure correct `enterpriseId` propagation.

## Implementation Details
Pointed all module-level `permissionServiceProvider`s to `unifiedPermissionServiceProvider`.
