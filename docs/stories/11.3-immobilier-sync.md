# Story 11.3: Multi-tenancy & Sync Integration

## Context
Ensuring all Immobilier data is synced and isolated by enterprise.

## Requirements
- Register Immobilier collections in `sync_paths.dart`.
- Ensure all repository queries filter by `enterpriseId`.
- Verify Firestore sync rules compatibility.

## Implementation Details
Update `lib/core/offline/sync_paths.dart`.
