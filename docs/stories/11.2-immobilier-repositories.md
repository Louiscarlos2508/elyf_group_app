# Story 11.2: Real Offline Repositories Implementation

## Context
Replacing `MockPropertyRepository` and other mocks with real Drift-backed implementations.

## Requirements
- Implement `OfflinePropertyRepository`.
- Implement `OfflineTenantRepository`.
- Implement `OfflineContractRepository`.
- Implement `OfflinePaymentRepository`.

## Implementation Details
Follow the pattern in `lib/features/eau_minerale/data/repositories/`.
